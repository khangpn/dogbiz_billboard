extends ../layout

block content
  include ./partials/_list_filter.jade
  div.panel.panel-default
    div.panel-heading
      h3.panel-title Dogs List
    div.panel-body
      div.table-responsive
        table.table.table-hover
          thead
            tr
              if isAdmin
                th ID
              th Chip ID
              th Name
              th Gender
              th Breed
              th Score
          tbody
            for dog in dogs
              tr
                if isAdmin
                  td
                    a(href='/dogs/' + dog.id)=dog.id
                td
                  a(href='/dogs/chipId/' + dog.chipId)=dog.chipId
                td
                  a(href='/dogs/' + dog.id)=dog.name
                td
                  =dog.gender ? "Male" : "Female"
                td
                  a(href='/breeds/' + dog.breed.fci)=dog.breed.name
                td
                  =dog.score
